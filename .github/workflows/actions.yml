name: 'CD-IntelDS-ddh-qa-v1'

on:
  schedule:
   - cron: '30 14 * * *' # Triggers every day at midnight UTC
  workflow_dispatch: # Allows manual triggering

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Display Hello World
      run: echo "Hello, World!"
